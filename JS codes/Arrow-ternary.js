const isEven = n => n % 2 === 0;
const result = marks >= 35 ? "Pass" : "Fail";
const greet = name => `Hello, ${name || "Guest"}`;
console.log(greet("Sunu"));  // Output: "Hello, Sunu"
console.log(greet());       

